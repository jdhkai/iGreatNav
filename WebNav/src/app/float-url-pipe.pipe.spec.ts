import { FloatUrlPipePipe } from './float-url-pipe.pipe';

describe('FloatUrlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FloatUrlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
